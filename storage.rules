rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow anyone to read uploaded imagery and published HTML cards
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null; // Still restrict uploads to logged in users
    }
  }
}
